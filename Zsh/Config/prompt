# If not running interactively, don't do anything
#case $- in
    #*i*) ;;
      #*) return;;
#esac

# Normal prompt
normal_prompt='%F{213}[%*]%f %F{212}(%h)%f %(?.%F{77}⏺.%F{9}⏺)%f %F{167}%n%f %F{35}@%f %F{38}%~%f %F{253}$%f '
