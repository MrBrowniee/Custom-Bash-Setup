This repository is a collection of configuration files for bash. The goal is cleaning up the home directory and .bashrc by sourcing everything from separate files

The .bashrc modifications assume that your configuration files are found in ~/.config/bash/

Default installation:

Config: 

1. Clone repository or download zip files
2. Create a symlink into the git git-directory/Config 